Changelog: i18n sweep and string centralization

Summary
-------
This change centralizes UI strings into `APP_STRINGS` and replaces hard-coded literals across the codebase (focused on `src/**`, excluding `coverage/`). It standardizes Spanish strings and prepares the app for easier localization.

What I changed
--------------
- Centralized and extended `src/constants/shared.ts` (`APP_STRINGS`): added keys for `AUTH`, `HOME`, `INFO`, `PAYMENT`, `CAMERA`, `LANG`, and more.
- Replaced hard-coded UI literals with `APP_STRINGS` in these areas:
  - `src/features/auth/**` (login, signup, code, email, changePassword, components)
  - `src/features/settings/**` (payment screens, profile screens, notifications)
  - `src/features/home/**` (home, feed, addPost, liked)
  - `src/features/info/**` (about, help, help data)
  - `src/components/ui/myButton/drawerBtn.tsx` (drawer labels + language submenu)
  - `src/app/(drawer)/features/camera.tsx` (camera permission strings)
  - `src/features/settings/components/*` (payment preview, item, fields)
  - Various UI components (inputs, code inputs) where labels/placeholders were replaced.

Files touched (representative)
------------------------------
- src/constants/shared.ts
- src/features/auth/screens/* (loginScreen.tsx, signupScreen.tsx, signupStepTwoScreen.tsx, changePasswordScreen.tsx, codeScreen.tsx, emailSreen.tsx)
- src/features/settings/screens/* (methodsScreen.tsx, addMethodsScreen.tsx, editProfileScreen.tsx, notificationScreen.tsx, profileDetailsScreen.tsx)
- src/features/settings/components/* (PaymentMethodItem.tsx, PaymentCardPreview.tsx, PaymentMethodField.tsx, PaymentMethodsEmptyState.tsx)
- src/features/home/screens/* (homeScreen.tsx, feedScreen.tsx, addPostScreen.tsx, likedScreen.tsx)
- src/features/info/* (aboutUsScreen.tsx, helpScreen.tsx, utils/*)
- src/components/ui/myButton/drawerBtn.tsx
- src/app/(drawer)/features/camera.tsx

Notes
-----
- I validated changed files with the project's error checker (`get_errors`) and found no immediate syntax/type errors in modified files.
- Dynamic/user-provided strings (e.g., `user?.description`, API error messages) were left intact.

Suggested Git workflow (run locally)
------------------------------------
# create a branch
git checkout -b i18n/sweep

# add changes
git add -A

# commit
git commit -m "i18n: centralize UI strings and replace literals across src (auth, settings, home, info, drawer, camera)"

# push branch
git push -u origin i18n/sweep

# create a PR using GitHub CLI (optional)
# Requires `gh` and repository remote configured
gh pr create --title "i18n: centralize UI strings and replace literals across src" \
  --body "See CHANGES_I18N_SWEEP.md for a summary of changes and verification steps." \
  --base main

Verification steps before merge
-------------------------------
- Run the app and test authentication flows, adding payment methods, camera permission flow, and help/about pages.
- Run linters/formatters (e.g., `npm run lint`, `npm run format`) if available.
- Run unit/types tests if present.

If you want I can also:
- Create the Git branch and open a PR on your GitHub repo (I will need the repo owner/name or access configured),
- Or run a broader regex sweep to catch more minor literals.

---
Generated by Copilot assistant: changes are saved in workspace files. Please review the PR diff locally before merging.
